<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.booklog.booklogbackend.mapper.UsersMapper">
    <insert id="insertUser" parameterType="com.booklog.booklogbackend.Model.vo.Users">
        INSERT INTO users (email, password, nickname)
        VALUES (#{email}, #{password}, #{nickname})
    </insert>

    <select id="findByEmail" parameterType="string" resultType="com.booklog.booklogbackend.Model.vo.Users">
        SELECT id, email, password, nickname
        FROM users
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM users
        WHERE email = #{email}
    </select>
</mapper>